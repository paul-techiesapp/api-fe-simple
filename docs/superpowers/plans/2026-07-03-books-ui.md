# Lesson-03 Books UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a minimal Vite + React + TypeScript app that lists books from lesson-02's live API and routes to a mock-data book detail page.

**Architecture:** A Vite React-TS SPA. `react-router-dom` maps `/` → `BookListPage` (fetches `GET /books`) and `/books/:id` → `BookDetailPage` (renders mock data). API access is isolated in `src/api/books.ts`; the `Book` contract lives in `src/types/book.ts`.

**Tech Stack:** Vite, React 18, TypeScript, react-router-dom.

## Global Constraints

- App directory: `lesson-03/` (sibling of `lesson-01`, `lesson-02`).
- API base URL comes from `import.meta.env.VITE_API_URL`, default `http://localhost:3000`.
- `Book` type must match lesson-02's Prisma shape with `include: { category: true }`.
- No test framework, no state library, no UI kit (YAGNI — teaching lesson).
- Package manager: **pnpm** (matches lesson-01/lesson-02). Use `pnpm install`, `pnpm build`, `pnpm dev` — never npm.
- Verification per task = `pnpm build` from `lesson-03/` (runs `tsc` + Vite build, non-interactive). The user runs `pnpm dev` in a separate tab for visual checks.
- `api/` is not a git repository — no commit steps. If the user later runs `git init`, commits can be added.
- The detail page uses **mock** data on purpose; the real `GET /books/:id` call is a documented student exercise, not implemented here.

---

### Task 1: Scaffold the Vite React-TS project

**Files:**
- Create: `lesson-03/package.json`
- Create: `lesson-03/vite.config.ts`
- Create: `lesson-03/tsconfig.json`
- Create: `lesson-03/tsconfig.node.json`
- Create: `lesson-03/index.html`
- Create: `lesson-03/.gitignore`
- Create: `lesson-03/.env`
- Create: `lesson-03/src/main.tsx` (temporary "hello" entry, replaced in Task 3)
- Create: `lesson-03/src/index.css`

**Interfaces:**
- Produces: a buildable Vite app whose entry is `src/main.tsx`, mounting into `<div id="root">` in `index.html`.

- [ ] **Step 1: Create `lesson-03/package.json`**

```json
{
  "name": "lesson-03",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
```

- [ ] **Step 2: Create `lesson-03/vite.config.ts`**

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
});
```

- [ ] **Step 3: Create `lesson-03/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tsbuildinfo",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 4: Create `lesson-03/tsconfig.node.json`**

```json
{
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "outDir": "./node_modules/.tmp/node",
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

> Note: `tsconfig.node.json` is a referenced composite project, so TypeScript
> requires it to emit (it may not set `noEmit`). `outDir` + `tsBuildInfoFile`
> redirect those generated files into `node_modules/.tmp` so the project root
> stays clean.

- [ ] **Step 5: Create `lesson-03/index.html`**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lesson 03 — Library Books</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 6: Create `lesson-03/.gitignore`**

```gitignore
node_modules
dist
*.local
.DS_Store
```

- [ ] **Step 7: Create `lesson-03/.env`**

```
VITE_API_URL=http://localhost:3000
```

- [ ] **Step 8: Create `lesson-03/src/index.css`**

```css
:root {
  font-family: system-ui, Avenir, Helvetica, Arial, sans-serif;
  line-height: 1.5;
  color: #1a1a1a;
  background: #f7f7f8;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
}

.container {
  max-width: 720px;
  margin: 0 auto;
  padding: 2rem 1rem;
}

h1 {
  font-size: 1.75rem;
  margin-bottom: 1.5rem;
}

.book-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  gap: 0.75rem;
}

.book-card {
  display: block;
  padding: 1rem 1.25rem;
  background: #fff;
  border: 1px solid #e5e5e8;
  border-radius: 10px;
  text-decoration: none;
  color: inherit;
  transition: border-color 0.15s ease, transform 0.15s ease;
}

.book-card:hover {
  border-color: #4f46e5;
  transform: translateY(-1px);
}

.book-card .title {
  font-weight: 600;
  font-size: 1.05rem;
}

.book-card .meta {
  color: #6b7280;
  font-size: 0.9rem;
  margin-top: 0.15rem;
}

.badge {
  display: inline-block;
  margin-top: 0.4rem;
  padding: 0.1rem 0.55rem;
  font-size: 0.75rem;
  background: #eef2ff;
  color: #4f46e5;
  border-radius: 999px;
}

.state {
  padding: 1.5rem;
  text-align: center;
  color: #6b7280;
}

.state.error {
  color: #b91c1c;
}

.back-link {
  display: inline-block;
  margin-bottom: 1rem;
  color: #4f46e5;
  text-decoration: none;
}

.detail dl {
  display: grid;
  grid-template-columns: max-content 1fr;
  gap: 0.5rem 1.25rem;
  background: #fff;
  border: 1px solid #e5e5e8;
  border-radius: 10px;
  padding: 1.25rem;
}

.detail dt {
  font-weight: 600;
  color: #6b7280;
}

.note {
  margin-top: 1rem;
  padding: 0.85rem 1rem;
  background: #fffbeb;
  border: 1px solid #fde68a;
  border-radius: 8px;
  font-size: 0.9rem;
  color: #92400e;
}
```

- [ ] **Step 9: Create temporary `lesson-03/src/main.tsx`**

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="container">
      <h1>Lesson 03 scaffold OK</h1>
    </div>
  </React.StrictMode>,
);
```

- [ ] **Step 10: Install dependencies and verify the build**

Run:
```bash
cd lesson-03 && pnpm install && pnpm build
```
Expected: install completes, then `tsc -b && vite build` succeeds and writes `dist/`. No TypeScript errors.

---

### Task 2: Add the Book type and API client

**Files:**
- Create: `lesson-03/src/types/book.ts`
- Create: `lesson-03/src/api/books.ts`

**Interfaces:**
- Produces:
  - `type BookCategory = { id: number; name: string }`
  - `type Book = { id: number; title: string; author: string; isbn: string | null; totalCopies: number; categoryId: number; category: BookCategory; createdAt: string; updatedAt: string }`
  - `fetchBooks(): Promise<Book[]>` — GETs `${VITE_API_URL}/books`, throws `Error` on non-2xx.

- [ ] **Step 1: Create `lesson-03/src/types/book.ts`**

```ts
/** A category a book belongs to (from lesson-02). */
export type BookCategory = {
  id: number;
  name: string;
};

/**
 * A book as returned by lesson-02's `GET /books`
 * (Prisma Book model with `include: { category: true }`).
 */
export type Book = {
  id: number;
  title: string;
  author: string;
  isbn: string | null;
  totalCopies: number;
  categoryId: number;
  category: BookCategory;
  createdAt: string;
  updatedAt: string;
};
```

- [ ] **Step 2: Create `lesson-03/src/api/books.ts`**

```ts
import type { Book } from '../types/book';

// Vite exposes only VITE_-prefixed env vars to the browser.
const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:3000';

/** Fetch every book from the lesson-02 API. */
export async function fetchBooks(): Promise<Book[]> {
  const res = await fetch(`${API_URL}/books`);
  if (!res.ok) {
    throw new Error(`Request failed with status ${res.status}`);
  }
  return res.json() as Promise<Book[]>;
}
```

- [ ] **Step 3: Verify the build**

Run:
```bash
cd lesson-03 && pnpm build
```
Expected: build succeeds, no TypeScript errors (unused exports are allowed because they are `export`ed).

---

### Task 3: Wire routing and the book list page (real API)

**Files:**
- Create: `lesson-03/src/pages/BookListPage.tsx`
- Modify: `lesson-03/src/main.tsx` (replace the temporary scaffold with the router)

**Interfaces:**
- Consumes: `fetchBooks()` from `src/api/books.ts`, `Book` from `src/types/book.ts`.
- Produces: routes `/` (`BookListPage`) and `/books/:id` (`BookDetailPage`, added in Task 4). Each list item links to `/books/${book.id}`.

- [ ] **Step 1: Create `lesson-03/src/pages/BookListPage.tsx`**

```tsx
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { fetchBooks } from '../api/books';
import type { Book } from '../types/book';

export function BookListPage() {
  const [books, setBooks] = useState<Book[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    fetchBooks()
      .then((data) => setBooks(data))
      .catch(() => setError('Could not load books. Is the backend running on :3000?'))
      .finally(() => setLoading(false));
  }, []);

  return (
    <div className="container">
      <h1>Library Books</h1>

      {loading && <div className="state">Loading books…</div>}
      {error && <div className="state error">{error}</div>}

      {!loading && !error && (
        <ul className="book-list">
          {books.map((book) => (
            <li key={book.id}>
              <Link className="book-card" to={`/books/${book.id}`}>
                <div className="title">{book.title}</div>
                <div className="meta">by {book.author}</div>
                <span className="badge">{book.category.name}</span>
              </Link>
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Replace `lesson-03/src/main.tsx` with the router**

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { createBrowserRouter, RouterProvider } from 'react-router-dom';
import { BookListPage } from './pages/BookListPage';
import { BookDetailPage } from './pages/BookDetailPage';
import './index.css';

const router = createBrowserRouter([
  { path: '/', element: <BookListPage /> },
  { path: '/books/:id', element: <BookDetailPage /> },
]);

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RouterProvider router={router} />
  </React.StrictMode>,
);
```

Note: this imports `BookDetailPage`, created in Task 4. The build will fail until Task 4 is done — these two tasks land together. If you must verify Task 3 alone, temporarily comment out the detail import/route.

- [ ] **Step 3: Verify after Task 4**

Deferred to Task 4 Step 3 (shared build), since `main.tsx` now depends on `BookDetailPage`.

---

### Task 4: Add the mock-data detail page and finish navigation

**Files:**
- Create: `lesson-03/src/data/mockBook.ts`
- Create: `lesson-03/src/pages/BookDetailPage.tsx`

**Interfaces:**
- Consumes: `Book` from `src/types/book.ts`; `useParams` from `react-router-dom`.
- Produces: `mockBook: Book`; `BookDetailPage` component (referenced by `main.tsx` from Task 3).

- [ ] **Step 1: Create `lesson-03/src/data/mockBook.ts`**

```ts
import type { Book } from '../types/book';

/**
 * Mock book used by the detail page.
 * STUDENT EXERCISE: replace usage of this with a real `GET /books/:id` call.
 */
export const mockBook: Book = {
  id: 0,
  title: 'The Pragmatic Programmer',
  author: 'Andrew Hunt & David Thomas',
  isbn: '9780201616224',
  totalCopies: 3,
  categoryId: 1,
  category: { id: 1, name: 'Programming' },
  createdAt: '2026-01-01T00:00:00.000Z',
  updatedAt: '2026-01-01T00:00:00.000Z',
};
```

- [ ] **Step 2: Create `lesson-03/src/pages/BookDetailPage.tsx`**

```tsx
import { Link, useParams } from 'react-router-dom';
import { mockBook } from '../data/mockBook';

export function BookDetailPage() {
  // The book id from the URL, e.g. /books/2 → id === "2".
  const { id } = useParams<{ id: string }>();

  // TODO(student): replace `mockBook` with a real GET /books/:id call
  // using `id` above, mirroring fetchBooks() in src/api/books.ts.
  const book = mockBook;

  return (
    <div className="container detail">
      <Link className="back-link" to="/">← Back to books</Link>
      <h1>{book.title}</h1>

      <dl>
        <dt>Route id</dt>
        <dd>{id}</dd>
        <dt>Author</dt>
        <dd>{book.author}</dd>
        <dt>ISBN</dt>
        <dd>{book.isbn ?? '—'}</dd>
        <dt>Category</dt>
        <dd>{book.category.name}</dd>
        <dt>Total copies</dt>
        <dd>{book.totalCopies}</dd>
      </dl>

      <div className="note">
        This page shows <strong>mock</strong> data. Exercise: fetch the real
        book with <code>GET /books/{id}</code> and render it here.
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Verify the full build**

Run:
```bash
cd lesson-03 && pnpm build
```
Expected: `tsc -b && vite build` succeeds with no errors; `main.tsx`, both pages, the API client, and mock data all typecheck together.

- [ ] **Step 4: Visual check (user, in their dev tab)**

With the backend running (and CORS enabled — Task 5), run `pnpm dev` and open the app. Expected: the list shows the 4 seeded books (1984, A Brief History of Time, To Kill a Mockingbird, Sapiens); clicking one navigates to `/books/:id` showing the route id and mock book; "← Back to books" returns to the list.

---

### Task 5: Enable CORS on the lesson-02 backend

**Files:**
- Modify: `lesson-02/src/main.ts`

**Interfaces:**
- Produces: lesson-02 responds with `Access-Control-Allow-Origin` so a browser app on port 5173 can call `GET /books`.

- [ ] **Step 1: Enable CORS in `lesson-02/src/main.ts`**

Add `app.enableCors();` immediately after `const app = await NestFactory.create(AppModule);`:

```ts
async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // Allow the lesson-03 browser app (Vite dev server) to call this API.
  app.enableCors();

  // Validate and transform incoming payloads against DTOs automatically.
  app.useGlobalPipes(
```

- [ ] **Step 2: Restart the backend (user)**

The user restarts the lesson-02 server (per project convention, they run it in a separate tab).

- [ ] **Step 3: Verify CORS from the browser app**

With lesson-03 `pnpm dev` running, load the list page. Expected: books load with no CORS error in the browser console. (CLI check: `curl -i -H "Origin: http://localhost:5173" http://localhost:3000/books` returns an `Access-Control-Allow-Origin` header.)

---

## Self-Review

- **Spec coverage:** Stack/scaffold → Task 1; `Book` type + `fetchBooks` + `.env` → Task 2; routing + list from real API → Task 3; mock detail page + navigation + student TODO → Task 4; backend CORS change → Task 5. Loading/error states → Task 3 Step 1. All spec sections covered.
- **Placeholder scan:** No TBD/TODO-as-work in the plan itself. The only `TODO(student)` is intentional deliverable content (the exercise marker).
- **Type consistency:** `Book`/`BookCategory` defined in Task 2 are used unchanged in Tasks 3 and 4; `fetchBooks()` signature is consistent between definition (Task 2) and use (Task 3); `mockBook` typed as `Book` (Task 4) matches the type from Task 2.
- **Cross-task build note:** Task 3's `main.tsx` imports Task 4's `BookDetailPage`; this is called out explicitly and both share the build verification in Task 4 Step 3.
